
<%= render 'layouts/errors', obj: @customer %>

<%= form_for(@customer) do |f| %>

<div class="field">
	<%= f.label :nickname %>
	<%= f.text_field :nickname, class: "nickname" %>
</div>

<%#ここに画像編集%>

<div class="actions">
	<%= f.submit %>
</div>

<% end %>
