<%= @tourist.name %></td>
<%= @tourist.overview %></td>
<%= attachment_image_tag(@tourist, :image, :fill, 70, 70, fallback: "noimage.jpg") %>


<p>登録スポット</p>
<% @spots.each do |spot| %>

    <p><%= attachment_image_tag(spot, :image, :fill, 70, 70, fallback: "noimage.jpg") %></p>

    <p><%= spot.name %></p>

    <p><%= spot.overview %></p>

    <p><%= spot.address %>

    <p><%= spot.tourist.name %></p>
    <% end %>
